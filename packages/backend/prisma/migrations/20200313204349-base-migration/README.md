# Migration `20200313204349-base-migration`

This migration has been generated by Yann Renaudin at 3/13/2020, 8:43:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "TrackSource" AS ENUM ('Spotify', 'Youtube');

CREATE TABLE "jukebox"."Playlist" (
    "description" text   ,
    "id" text  NOT NULL ,
    "name" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
)

CREATE TABLE "jukebox"."Suggestion" (
    "id" text  NOT NULL ,
    "submitter" text  NOT NULL DEFAULT '',
    "track" text  NOT NULL ,
    PRIMARY KEY ("id")
)

CREATE TABLE "jukebox"."Artist" (
    "id" text  NOT NULL ,
    "name" text  NOT NULL DEFAULT '',
    "track" text   ,
    PRIMARY KEY ("id")
)

CREATE TABLE "jukebox"."Track" (
    "id" text  NOT NULL ,
    "source" "TrackSource" NOT NULL DEFAULT 'Spotify',
    "url" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
)

ALTER TABLE "jukebox"."Suggestion" ADD FOREIGN KEY ("track")REFERENCES "jukebox"."Track"("id") ON DELETE RESTRICT  ON UPDATE CASCADE

ALTER TABLE "jukebox"."Artist" ADD FOREIGN KEY ("track")REFERENCES "jukebox"."Track"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200313204349-base-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,39 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Playlist {
+  id          String  @default(cuid()) @id
+  name        String
+  description String?
+}
+
+model Suggestion {
+  id        String @default(cuid()) @id
+  submitter String
+  track     Track
+}
+
+model Artist {
+  id   String @default(cuid()) @id
+  name String
+}
+
+model Track {
+  id      String      @default(cuid()) @id
+  url     String
+  source  TrackSource
+  artists Artist[]
+}
+
+enum TrackSource {
+  Spotify Youtube
+}
```
